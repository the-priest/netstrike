#!/bin/bash
# NetStrike GUI launcher
# Uses sudo -E to preserve the full user env (Wayland socket, dbus, etc.).
# Relies on /etc/sudoers.d/netstrike for passwordless root.
exec sudo -E /usr/local/bin/netstrike "$@"
